#!/bin/bash
# Двойной клик в Finder -> запустит скрипт в Терминале.
cd "$(dirname "$0")"
python3 поправка_ПЦН.py
echo ""
echo "=== Готово. Результат в папке \"результат ...\" рядом. ==="
read -n1 -r -p "Нажми любую клавишу для выхода..."
